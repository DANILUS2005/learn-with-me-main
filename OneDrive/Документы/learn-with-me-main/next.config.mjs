/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    
  },
  images: {
    domains: ['localhost', 'example.com', 'another-domain.com'], // Добавьте другие домены, если необходимо
  },
};
  
  export default nextConfig;